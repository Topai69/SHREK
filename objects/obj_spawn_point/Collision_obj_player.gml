// When the player collides with the checkpoint, store the position and player data
global.checkpoint_x = x;  // Store the checkpoint's x coordinate
global.checkpoint_y = y;  // Store the checkpoint's y coordinate
